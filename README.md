# Gemini Proxy 2

Clean Vercel serverless proxy package for the apps assigned to Gemini Proxy 2.

Required Vercel environment variable:

`GEMINI_API_KEY`

The repository root must contain `package.json`, `vercel.json`, and the `api` folder. Keep the Vercel Root Directory setting empty.
